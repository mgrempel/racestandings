<h1>Seasons#index</h1>

<%= render 'layouts/search'%>

<table>
  <% @seasons.each do |season| %>
    <tr>
      <td><%= link_to season.year,  season_path(season)%></td>
    </tr>
  <% end %>
</table>
