<div class="ml-4">
  <h3 class="title"><%= @laptime.laptime %></h3>

  <p>Set by <%=link_to @laptime.driver.first_name, driver_path(@laptime.driver)%>
  <%=link_to @laptime.driver.last_name, driver_path(@laptime.driver)%> on
  <%=link_to @laptime.circuit.name, circuit_path(@laptime.circuit)%> in
  <%=link_to @laptime.season.year, season_path(@laptime.season)%></p>
</div>